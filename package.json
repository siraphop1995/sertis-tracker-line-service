{
  "name": "sertis-tracker-line-service",
  "version": "1.0.0",
  "description": "Prototype project for sertise tracker",
  "main": "server.js",
  "scripts": {
    "preinstall": "npm install pm2 -g",
    "start": "pm2-runtime start ecosystem.config.js --env production",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "pm2 start ecosystem.config.js",
    "stop": "pm2 delete ecosystem.config.js",
    "log": "pm2 log line"
  },
  "author": "siraphop",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "http-status": "^1.3.2",
    "mongoose": "^5.6.10"
  }
}
